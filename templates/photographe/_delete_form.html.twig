<form method="post" action="{{ path('app_photographe_delete', {'id': photographe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ photographe.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Photographe{% endblock %}

{% block body %}

    <h1>Photographe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photographe.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ photographe.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ photographe.roles ? photographe.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ photographe.password }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ photographe.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ photographe.Prenom }}</td>
            </tr>
            <tr>
                <th>TVA</th>
                <td>{{ photographe.TVA }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ photographe.Photo }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ photographe.CodePostal }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ photographe.Pays }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_photographe_index') }}">back to list</a>

    <a href="{{ path('app_photographe_edit', {'id': photographe.id}) }}">edit</a>

    {{ include('photographe/_delete_form.html.twig') }}
{% endblock %}
